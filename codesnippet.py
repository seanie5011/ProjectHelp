import numpy as np

# FUNCTIONS

def add_two_arrays(a, b):
	'''
	Function to add two arrays, returning: a + b
	'''

	# return the addition of a and b
	return a + b

# MAIN LOOP

if __name__ == '__main__':
	# define two arrays
	a = np.array([1, 2, 3])
	a = np.array([1, 2, 3])

	# add them and print the result
	added = add_two_arrays(a, b)
	print(added)